---
phase: 04-social-feed-feature
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - lib/features/social/data/repositories/post_repository_impl.dart
autonomous: false

must_haves:
  truths:
    - "Repository queries use Brick cache with alwaysHydrate policy"
    - "Feed loads instantly from cache when offline"
    - "Posts sync automatically from Supabase when online"
    - "Combined feed includes both stories and posts sorted by date"
  artifacts:
    - path: "lib/features/social/data/repositories/post_repository_impl.dart"
      provides: "Brick-based repository with network-first caching"
      contains: "repository.get<ImagePostModel>"
    - path: "lib/features/social/data/repositories/post_repository_impl.dart"
      provides: "alwaysHydrate policy for feed queries"
      contains: "OfflineFirstGetPolicy.alwaysHydrate"
  key_links:
    - from: "lib/features/social/data/repositories/post_repository_impl.dart"
      to: "MyItihasRepository.get<T>"
      via: "Brick query with alwaysHydrate policy"
      pattern: "repository\\.get<.*PostModel>\\("
    - from: "lib/features/social/data/repositories/post_repository_impl.dart"
      to: "ImagePostModel.toDomain()"
      via: "Model to entity conversion"
      pattern: "\\.toDomain\\(\\)"
---

<objective>
Replace PostService direct calls with Brick repository queries for automatic offline caching of feed posts.

Purpose: Enable feed browsing without internet using cached posts and profiles
Output: Updated PostRepositoryImpl using Brick for all post queries with network-first policy
</objective>

<execution_context>
@/Users/amansikarwar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amansikarwar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-social-feed-feature/04-CONTEXT.md
@.planning/phases/04-social-feed-feature/04-RESEARCH.md
@.planning/phases/02-stories-feature/02-02-SUMMARY.md
@lib/features/social/data/repositories/post_repository_impl.dart
@lib/features/stories/data/repositories/story_repository_impl.dart
</context>

<tasks>

<task type="auto">
  <name>Update PostRepositoryImpl to use Brick caching</name>
  <files>lib/features/social/data/repositories/post_repository_impl.dart</files>
  <action>
Replace PostService calls with Brick repository queries:

1. Add MyItihasRepository to constructor:
   - Import: package:myitihas/repository/my_itihas_repository.dart
   - Import Brick models: package:myitihas/features/social/data/models/image_post.model.dart (and text, video)
   - Add field: final MyItihasRepository _repository
   - Add to constructor parameters

2. Update getImagePosts method:
   - Replace _postService.getFeed() call with:
   ```dart
   final models = await _repository.get&lt;ImagePostModel&gt;(
     policy: OfflineFirstGetPolicy.alwaysHydrate,
     query: Query.where('post_type', 'image')
       .orderBy('created_at', descending: true)
       .offset(offset)
       .limit(limit),
   );
   ```
   - Convert to domain: final imagePosts = models.map((m) =&gt; m.toDomain()).toList();
   - Return Right(imagePosts)
   - Wrap in try-catch catching BrickException =&gt; Left(CacheFailure(e.message))

3. Update getTextPosts method:
   - Same pattern, query where('post_type', 'text')
   - Convert with TextPostModel.toDomain()

4. Update getVideoPosts method:
   - Same pattern, query where('post_type', 'video')
   - Convert with VideoPostModel.toDomain()

5. Update getAllFeedItems method:
   - Keep existing _storyRepository.getStories() call (already uses Brick)
   - Replace _postService.getFeed() with THREE parallel Brick queries:
     * await _repository.get&lt;ImagePostModel&gt;(policy: alwaysHydrate, query: where('post_type', 'image').orderBy('created_at', descending: true))
     * await _repository.get&lt;TextPostModel&gt;(policy: alwaysHydrate, query: where('post_type', 'text').orderBy('created_at', descending: true))
     * await _repository.get&lt;VideoPostModel&gt;(policy: alwaysHydrate, query: where('post_type', 'video').orderBy('created_at', descending: true))
   - Convert each to FeedItems: FeedItem.imagePost(m.toDomain()), etc.
   - Combine with stories, sort by date, apply pagination
   - Keep existing error handling

6. Update getImagePostById, getTextPostById, getVideoPostById:
   - Replace _postService.getPostById() with:
   ```dart
   final models = await _repository.get&lt;ImagePostModel&gt;(
     policy: OfflineFirstGetPolicy.awaitRemoteWhenNoneExist,
     query: Query.where('id', id),
   );
   if (models.isEmpty) {
     return Left(NotFoundFailure('Image post not found', 'NOT_FOUND'));
   }
   return Right(models.first.toDomain());
   ```

7. Remove or keep PostService field:
   - If PostService still used elsewhere, keep _postService field
   - If only used here, remove from constructor (check usages first)

WHY alwaysHydrate: Network-first with cache fallback per user decision
WHY filter by post_type: Posts table stores all types, need discrimination
WHY awaitRemoteWhenNoneExist for details: Ensure fresh data on first access
  </action>
  <verify>
Repository imports MyItihasRepository and Brick models
grep "MyItihasRepository _repository" lib/features/social/data/repositories/post_repository_impl.dart
grep "repository.get&lt;ImagePostModel&gt;" lib/features/social/data/repositories/post_repository_impl.dart
grep "OfflineFirstGetPolicy.alwaysHydrate" lib/features/social/data/repositories/post_repository_impl.dart
grep "where('post_type'" lib/features/social/data/repositories/post_repository_impl.dart
flutter analyze lib/features/social/data/repositories/
  </verify>
  <done>
PostRepositoryImpl uses Brick queries with alwaysHydrate for feed, filters by post_type, converts models to domain entities via toDomain()
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete offline-first social feed with Brick models, realtime subscriptions, and network-aware UI
  </what-built>
  <how-to-verify>
Test full social feed offline capability:

1. **Load Feed While Online**
   - Open MyItihas app
   - Navigate to social feed
   - Verify posts load (stories + image/text/video posts)
   - Verify author profiles display correctly
   - Expected: Feed displays with mixed content types

2. **Browse Feed Offline**
   - Enable airplane mode or disconnect WiFi
   - Close and reopen app (or pull-to-refresh)
   - Scroll through feed
   - Tap on post to view details
   - Expected: Previously loaded posts display instantly from cache
   - Expected: Author profiles show correctly from cache
   - Expected: Engagement counts (likes, comments, shares) display

3. **Attempt Write Action Offline**
   - While still offline, tap like button on a post
   - Expected: Error snackbar appears: "No internet connection. Try again later."
   - Expected: Like action does NOT execute
   - Expected: Snackbar has "Dismiss" action
   - Tap dismiss
   - Expected: Snackbar disappears

4. **Realtime Updates When Online**
   - Reconnect to internet
   - Wait 5-10 seconds
   - Create a new post from another device/session (if possible, otherwise skip)
   - Expected: New post appears at top of feed automatically
   - Expected: No manual refresh needed
   - (If cannot test realtime, skip this scenario)

5. **Verify Translations**
   - Change app language to Hindi, Tamil, or Telugu
   - Attempt offline write action
   - Expected: Error message appears in selected language
   - Expected: "Dismiss" button translated

6. **Post Type Filtering**
   - Verify feed shows mix of:
     * Stories (from Phase 2 Brick implementation)
     * Image posts (with images displaying)
     * Text posts (with styled text)
     * Video posts (with thumbnails)
   - Expected: All post types render correctly
   - Expected: Feed sorted by date (newest first)

**Pass criteria:**
- Feed loads from cache offline (scenarios 2, 6)
- Write actions blocked offline with snackbar (scenario 3)
- Translations work (scenario 5)
- No crashes or analyzer errors
  </how-to-verify>
  <resume-signal>
Type "approved" if all scenarios pass, or describe any issues encountered
  </resume-signal>
</task>

</tasks>

<verification>
Run flutter analyze on social feature
Test feed loading with and without internet
Verify Brick queries execute without errors
Test realtime subscription behavior
</verification>

<success_criteria>
- PostRepositoryImpl imports MyItihasRepository and post models
- All feed query methods use repository.get&lt;T&gt; with OfflineFirstGetPolicy.alwaysHydrate
- Post type filtering via Query.where('post_type', value)
- Models converted to domain entities via .toDomain()
- BrickException caught and mapped to CacheFailure
- Feed works offline with previously loaded posts
- Write actions show error snackbar when offline
- Realtime subscriptions insert new posts automatically
- All post types (image, text, video) display correctly in feed
</success_criteria>

<output>
After completion, create `.planning/phases/04-social-feed-feature/04-03-SUMMARY.md`
</output>
