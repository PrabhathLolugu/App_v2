---
phase: 02-stories-feature
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/stories/data/models/story.model.dart
  - lib/features/stories/data/models/story_attributes.model.dart
  - lib/features/stories/data/repositories/story_repository_impl.dart
  - lib/repository/my_itihas_repository.dart
autonomous: false

must_haves:
  truths:
    - "User can view previously loaded story lists without internet connection"
    - "User can open and read story details for any cached story while offline"
    - "Story data syncs automatically from Supabase when online"
    - "Stories persist in SQLite across app restarts"
  artifacts:
    - path: "lib/features/stories/data/models/story.model.dart"
      provides: "Brick model with @ConnectOfflineFirstWithSupabase for Story entity"
      contains: "@ConnectOfflineFirstWithSupabase"
    - path: "lib/features/stories/data/models/story_attributes.model.dart"
      provides: "Brick model for nested StoryAttributes data"
      contains: "@ConnectOfflineFirstWithSupabase"
    - path: "lib/features/stories/data/repositories/story_repository_impl.dart"
      provides: "Repository implementation using Brick queries"
      pattern: "repository\\.get<StoryModel>"
    - path: "lib/repository/my_itihas_repository.dart"
      provides: "Story models registered in modelDictionary"
      pattern: "StoryModel.*StoryAttributesModel"
  key_links:
    - from: "lib/features/stories/data/repositories/story_repository_impl.dart"
      to: "lib/repository/my_itihas_repository.dart"
      via: "repository.get<StoryModel>() queries"
      pattern: "repository\\.get<StoryModel>\\("
    - from: "lib/features/stories/data/models/story.model.dart"
      to: "Supabase stories table"
      via: "@ConnectOfflineFirstWithSupabase annotation"
      pattern: "tableName: 'stories'"
    - from: "lib/features/stories/data/models/story.model.dart"
      to: "lib/features/stories/domain/entities/story.dart"
      via: "toDomain() method"
      pattern: "Story toDomain\\(\\)"
---

<objective>
Migrate Stories feature from Hive to Brick offline-first architecture, enabling users to browse stories and view details offline for previously loaded content.

Purpose: Replace Hive-based local storage with Brick models that provide automatic Supabase sync, offline-first queries, and SQLite persistence. This is the first feature migration that establishes the pattern for subsequent phases.

Output: Brick models for Story and StoryAttributes, updated repository implementation using MyItihasRepository queries, and verified offline story browsing capability.
</objective>

<execution_context>
@/Users/amansikarwar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amansikarwar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-infrastructure/01-01-SUMMARY.md
@.planning/phases/02-stories-feature/02-CONTEXT.md
@.planning/phases/02-stories-feature/02-RESEARCH.md
@lib/features/stories/domain/entities/story.dart
@lib/features/stories/data/models/story_model.dart
@lib/features/stories/data/repositories/story_repository_impl.dart
@lib/repository/my_itihas_repository.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Brick Models for Story and StoryAttributes</name>
  <files>
    lib/features/stories/data/models/story.model.dart
    lib/features/stories/data/models/story_attributes.model.dart
  </files>
  <action>
Replace existing Hive models with Brick models extending OfflineFirstWithSupabaseModel.

**For story.model.dart:**
- Remove @freezed and @HiveType annotations, replace with @ConnectOfflineFirstWithSupabase
- Configure `supabaseConfig: SupabaseSerializable(tableName: 'stories')`
- Extend `OfflineFirstWithSupabaseModel` instead of using Hive mixins
- Annotate `id` field with `@Supabase(unique: true)` for primary key
- Use `@Supabase(name: 'image_url')` for snake_case column mapping where needed
- Add fields from domain entity: authorId, createdAt, updatedAt (snake_case auto-converts)
- Remove `fromJson`/`toJson` factory (Brick generates these)
- Keep `toDomain()` method (rename from `toEntity()`) for domain entity conversion
- Remove `fromEntity()` factory (not needed in Brick pattern)
- Use `final StoryAttributesModel attributes` for nested model association

**For story_attributes.model.dart:**
- Create new file extending OfflineFirstWithSupabaseModel
- Configure `supabaseConfig: SupabaseSerializable(tableName: 'story_attributes')`
- Annotate `id` field with `@Supabase(unique: true)`
- Map all snake_case columns: story_type, main_character_type, story_setting, time_era, narrative_perspective, language_style, emotional_tone, narrative_style, plot_structure, story_length
- Include List<String> fields: references, tags
- Add `toDomain()` method returning StoryAttributes entity
- DO NOT include characters, characterDetails, or translations fields (these are in domain entity but not in current Supabase schema based on existing Hive model)

**Important:**
- Do NOT use @freezed with Brick models (incompatible)
- Do NOT include build runner generation parts (part 'story.model.g.dart') - Brick generates adapters separately
- Follow the exact pattern from 02-RESEARCH.md code examples
- Keep constructor simple (no freezed unions or factories)
  </action>
  <verify>
Check that:
- story.model.dart has @ConnectOfflineFirstWithSupabase annotation with tableName: 'stories'
- story_attributes.model.dart has @ConnectOfflineFirstWithSupabase annotation with tableName: 'story_attributes'
- Both models extend OfflineFirstWithSupabaseModel
- Primary key fields have @Supabase(unique: true)
- toDomain() methods exist and return domain entities
- No @freezed, @HiveType, or Hive-related code remains
- Files compile without build_runner (models defined, adapters generated later)
  </verify>
  <done>
Brick model files created following OfflineFirstWithSupabaseModel pattern, with correct Supabase table mapping, primary key annotations, and toDomain() conversion methods. Models are ready for build_runner code generation.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update StoryRepositoryImpl to Use Brick Queries</name>
  <files>
    lib/features/stories/data/repositories/story_repository_impl.dart
  </files>
  <action>
Replace Hive datasource calls with MyItihasRepository Brick queries.

**Changes to StoryRepositoryImpl:**
1. **Update constructor:**
   - Remove `StoryRemoteDataSource remoteDataSource`
   - Remove `StoryLocalDataSource localDataSource`
   - Remove `NetworkInfo networkInfo`
   - Add `required MyItihasRepository repository` parameter
   - Keep `@LazySingleton(as: StoryRepository)` annotation

2. **Update getStories() method:**
   - Replace `localDataSource.getCachedStories()` with:
     ```dart
     final models = await repository.get<StoryModel>(
       policy: OfflineFirstGetPolicy.alwaysHydrate,
     );
     ```
   - Keep existing filtering/sorting/pagination logic
   - Change `model.toEntity()` to `model.toDomain()`
   - Wrap in try-catch with `BrickException` handling

3. **Update getStoryById() method:**
   - Replace datasource call with:
     ```dart
     final models = await repository.get<StoryModel>(
       policy: OfflineFirstGetPolicy.awaitRemoteWhenNoneExist,
       query: Query.where('id', id),
     );
     ```
   - Change `cachedStory.toEntity()` to `models.first.toDomain()`

4. **Update getFavoriteStories() method:**
   - Use `repository.get<StoryModel>()` with `Query.where('isFavorite', true)`
   - Policy: `OfflineFirstGetPolicy.localOnly` (favorites are local-only)

5. **Update toggleFavorite() method:**
   - Fetch story with `repository.get<StoryModel>()` using `localOnly` policy
   - Create updated StoryModel with toggled `isFavorite` field
   - Call `await repository.upsert<StoryModel>(updated, policy: OfflineFirstUpsertPolicy.optimisticLocal)`

6. **Update getTrendingStories(), getStoriesByScripture(), searchStories() methods:**
   - Replace datasource calls with `repository.get<StoryModel>(policy: OfflineFirstGetPolicy.alwaysHydrate)`
   - Keep filtering logic, change `.toEntity()` to `.toDomain()`

7. **Update cacheStories() method:**
   - Remove UnimplementedError
   - Use `repository.upsert<StoryModel>()` to cache each story

8. **Update getCachedStories() method:**
   - Use `repository.get<StoryModel>(policy: OfflineFirstGetPolicy.localOnly)`

**Imports to add:**
- `import 'package:brick_offline_first/brick_offline_first.dart';`
- `import 'package:myitihas/repository/my_itihas_repository.dart';`
- `import '../models/story.model.dart';`
- `import '../models/story_attributes.model.dart';`

**Imports to remove:**
- `import '../datasources/story_local_data_source.dart';`
- `import '../datasources/story_remote_data_source.dart';`
- `import 'package:myitihas/core/network/network_info.dart';`

**Error handling:**
- Catch `BrickException` instead of `CacheException`
- Return `Left(CacheFailure(e.message))` for Brick errors
  </action>
  <verify>
Run:
```bash
flutter analyze lib/features/stories/data/repositories/story_repository_impl.dart
```

Check that:
- No references to StoryLocalDataSource or StoryRemoteDataSource remain
- All methods use `repository.get<StoryModel>()` or `repository.upsert<StoryModel>()`
- Correct OfflineFirstGetPolicy used: alwaysHydrate for lists, awaitRemoteWhenNoneExist for details, localOnly for favorites
- All `.toEntity()` calls changed to `.toDomain()`
- File compiles (may have missing import errors until Task 3 generates adapters)
  </verify>
  <done>
StoryRepositoryImpl migrated to Brick queries, using MyItihasRepository instead of Hive datasources. All methods use appropriate offline-first policies and convert Brick models to domain entities via toDomain().
  </done>
</task>

<task type="auto">
  <name>Task 3: Register Models and Generate Brick Adapters</name>
  <files>
    lib/repository/my_itihas_repository.dart
  </files>
  <action>
Update MyItihasRepository to include Story models in modelDictionary and run build_runner.

**Update my_itihas_repository.dart:**
1. Add imports:
   ```dart
   import 'package:myitihas/features/stories/data/models/story.model.dart';
   import 'package:myitihas/features/stories/data/models/story_attributes.model.dart';
   ```

2. Update `modelDictionary` in `configure()` method from `{}` to:
   ```dart
   modelDictionary: {
     StoryModel: {},
     StoryAttributesModel: {},
   }
   ```

3. Save file and run code generation:
   ```bash
   dart run build_runner build --delete-conflicting-outputs
   ```

This generates:
- `lib/brick.g.dart` - Model dictionary and adapter registry
- Adapter files for StoryModel and StoryAttributesModel
- SQLite and Supabase serialization code

**Expected output:**
- Build runner should complete successfully
- Check for `[INFO] Succeeded after Xs` message
- Verify `lib/brick.g.dart` contains StoryModel and StoryAttributesModel entries

**If build fails:**
- Check model annotations match @ConnectOfflineFirstWithSupabase pattern exactly
- Verify tableName is specified in SupabaseSerializable
- Ensure all required fields are present
- Check that @Supabase(unique: true) is on id fields
  </action>
  <verify>
Run:
```bash
# Check build_runner output
dart run build_runner build --delete-conflicting-outputs

# Verify generated files exist
ls lib/brick.g.dart

# Check app compiles
flutter analyze

# Verify repository initializes
flutter run --debug
```

Check that:
- build_runner completes without errors
- lib/brick.g.dart contains StoryModel and StoryAttributesModel
- No compilation errors in StoryRepositoryImpl
- App starts successfully with Brick repository initialized
- Talker logs show "MyItihas repository initialized" on startup
  </verify>
  <done>
Story models registered in MyItihasRepository modelDictionary, Brick adapters generated via build_runner, app compiles and runs with Story models integrated into offline-first architecture.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Stories feature migration to Brick offline-first architecture. Story and StoryAttributes Brick models created with Supabase table mapping, repository updated to use Brick queries with appropriate policies, and build_runner generated adapters for SQLite/Supabase serialization.
  </what-built>
  <how-to-verify>
1. **Verify app startup:**
   - Run `flutter run --debug` on device/emulator
   - Check console for "MyItihas repository initialized" log message
   - Ensure no Brick-related errors during initialization

2. **Test story list loading:**
   - Open the app and navigate to Stories feature
   - Observe story list loads (may be empty if no cached data, or shows existing Hive cache)
   - Expected: UI displays without errors

3. **Test offline browsing (requires existing data):**
   - If stories are visible, enable Airplane Mode on device
   - Navigate away from Stories and back
   - Expected: Cached stories still display offline
   - Note: If no stories cached yet, skip this step (will test after data sync in Phase 7)

4. **Test story detail view:**
   - Tap any story card to open detail view
   - Expected: Story detail page opens without errors
   - Expected: All story fields (title, scripture, story text, etc.) display correctly

5. **Check for regressions:**
   - Verify no crashes when browsing stories
   - Check that story cards render correctly (image placeholders OK)
   - Ensure navigation back from detail view works

**Success criteria:**
- App starts without Brick initialization errors
- Story list page renders without crashes
- Story detail view displays all text content
- No console errors related to model serialization or repository queries

**Known limitations at this stage:**
- No data sync from Supabase yet (Phase 7)
- Images may not load (media caching is Phase 6)
- May show empty state if no Hive data exists (migration/cleanup is Phase 8)
  </how-to-verify>
  <resume-signal>
Type "approved" if verification passed, or describe specific issues encountered (errors, crashes, unexpected behavior).
  </resume-signal>
</task>

</tasks>

<verification>
After all tasks complete:

1. **Model verification:**
   - `grep -r "@ConnectOfflineFirstWithSupabase" lib/features/stories/data/models/`
   - Should show story.model.dart and story_attributes.model.dart

2. **Repository verification:**
   - `grep "repository.get<StoryModel>" lib/features/stories/data/repositories/story_repository_impl.dart`
   - Should show multiple Brick query usages

3. **Build artifacts:**
   - `ls lib/brick.g.dart`
   - Should exist and contain StoryModel references

4. **Compilation:**
   - `flutter analyze`
   - Should pass with no errors in stories feature

5. **Runtime:**
   - `flutter run`
   - Should initialize repository and load stories without Brick errors
</verification>

<success_criteria>
- Brick models created with correct @ConnectOfflineFirstWithSupabase annotations and Supabase table mapping
- StoryRepositoryImpl uses MyItihasRepository.get<StoryModel>() instead of Hive datasources
- Appropriate OfflineFirstGetPolicy used for each repository method (alwaysHydrate for lists, awaitRemoteWhenNoneExist for details, localOnly for favorites)
- modelDictionary in MyItihasRepository includes StoryModel and StoryAttributesModel
- build_runner successfully generates Brick adapters in lib/brick.g.dart
- App compiles and runs without Brick-related errors
- Story list and detail views render correctly
- User can browse cached stories (if data exists from previous Hive cache)
</success_criteria>

<output>
After completion, create `.planning/phases/02-stories-feature/02-01-SUMMARY.md` following the summary template with:
- Performance metrics (duration, tasks completed)
- Files created/modified
- Key decisions made during implementation
- Any deviations from plan
- Verification results from checkpoint
- Readiness assessment for next plans
</output>
