---
phase: 08-migration-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/migration/hive_migration_service.dart
  - lib/main.dart
autonomous: true

must_haves:
  truths:
    - "App detects first launch after migration and initiates cleanup"
    - "Hive directory is deleted on first launch if it exists"
    - "Migration runs in background without blocking app launch"
    - "Migration status is tracked to prevent repeated cleanup attempts"
  artifacts:
    - path: "lib/core/migration/hive_migration_service.dart"
      provides: "Migration detection and Hive cleanup logic"
      min_lines: 60
      exports: ["HiveMigrationService"]
    - path: "lib/main.dart"
      provides: "Migration service integration"
      contains: "migrationService.runMigration()"
  key_links:
    - from: "lib/main.dart"
      to: "HiveMigrationService"
      via: "getIt dependency injection"
      pattern: "getIt<HiveMigrationService>"
    - from: "lib/core/migration/hive_migration_service.dart"
      to: "SharedPreferences"
      via: "migration status tracking"
      pattern: "prefs\\.getBool.*hive_migration_complete"
    - from: "lib/core/migration/hive_migration_service.dart"
      to: "Directory.delete"
      via: "recursive directory cleanup"
      pattern: "delete\\(recursive: true\\)"
---

<objective>
Create background migration service that silently cleans up Hive data on user devices.

Purpose: Removes legacy Hive storage from user devices as part of Hive → Brick migration, ensuring clean transition without blocking app launch.
Output: HiveMigrationService integrated in main.dart, running non-blocking cleanup on first launch after migration.
</objective>

<execution_context>
@/Users/amansikarwar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amansikarwar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-migration-cleanup/08-CONTEXT.md
@.planning/phases/08-migration-cleanup/08-RESEARCH.md
@lib/core/cache/services/cache_cleanup_service.dart
@lib/main.dart
</context>

<tasks>

<task type="auto">
  <name>Create HiveMigrationService with background cleanup</name>
  <files>lib/core/migration/hive_migration_service.dart</files>
  <action>
Create a new service following the CacheCleanupService pattern (from main.dart:49-53). The service must:

1. Use `@lazySingleton` annotation for injectable DI
2. Define two SharedPreferences keys:
   - `hive_migration_complete` (bool) - tracks successful completion
   - `hive_migration_failed_count` (int) - tracks retry attempts

3. Implement `runMigration()` method that:
   - Checks if migration already completed via SharedPreferences
   - Gets Hive directory path using `path_provider.getApplicationDocumentsDirectory()` + `/hive` subdirectory
   - Returns early if no Hive directory exists (marks as complete)
   - Calls internal cleanup method if directory exists

4. Implement `_cleanupHiveData(Directory hiveDir, SharedPreferences prefs)` private method that:
   - Deletes Hive directory recursively using `Directory.delete(recursive: true)`
   - Logs success with `talker.good()`
   - Sets `hive_migration_complete` to true and removes `hive_migration_failed_count`
   - On error: checks retry count, retries once with 2-second delay, abandons after second failure
   - On abandon: logs error, marks as complete to prevent infinite retries

5. Use proper error handling with try/catch, stack traces logged via talker
6. Follow exact code structure from RESEARCH.md "Complete Migration Service" section

Imports needed: dart:io (Directory), injectable, shared_preferences, path_provider, talker_setup
  </action>
  <verify>
Run `flutter analyze` - no errors in lib/core/migration/hive_migration_service.dart
Grep for @lazySingleton annotation exists in the file
Grep for both SharedPreferences keys defined as constants
  </verify>
  <done>
HiveMigrationService exists with runMigration() method, uses SharedPreferences tracking, deletes Hive directory recursively, has retry logic with max 1 retry
  </done>
</task>

<task type="auto">
  <name>Integrate migration service in main.dart</name>
  <files>lib/main.dart</files>
  <action>
Update main.dart to run Hive migration cleanup after dependency injection but before runApp():

1. Import the new service: `import 'package:myitihas/core/migration/hive_migration_service.dart';`

2. Add migration cleanup AFTER `await configureDependencies();` and BEFORE the existing `CacheCleanupService` call:
```dart
// Run Hive migration cleanup (non-blocking)
final migrationService = getIt<HiveMigrationService>();
migrationService.runMigration().catchError((e) {
  talker.error('Hive migration failed', e);
});
```

3. Place it in the exact position shown in RESEARCH.md "Integration in main.dart" section

DO NOT remove or modify the `await initHive();` call yet - that will be removed in Plan 02.
DO NOT change any other initialization code.
  </action>
  <verify>
Run `flutter analyze` - no errors
Grep for "migrationService.runMigration()" exists in main.dart
Grep confirms migration cleanup appears BEFORE CacheCleanupService
Grep confirms initHive() still exists (not removed yet)
  </verify>
  <done>
main.dart calls HiveMigrationService.runMigration() in background with catchError, positioned after DI setup and before CacheCleanupService
  </done>
</task>

<task type="auto">
  <name>Regenerate DI and test migration service</name>
  <files>lib/core/di/injection_container.config.dart</files>
  <action>
Regenerate dependency injection configuration to register the new HiveMigrationService:

1. Run: `dart run build_runner build --delete-conflicting-outputs`
2. Verify HiveMigrationService is registered in injection_container.config.dart
3. Run: `flutter analyze` to confirm no compilation errors
4. Run app in test mode (if possible) or just verify it compiles: `flutter run --debug` (can terminate immediately after launch)

The migration service will run on first launch, detect no Hive directory exists (since all features already migrated to Brick), and mark migration as complete.
  </action>
  <verify>
Grep injection_container.config.dart contains "HiveMigrationService"
Run `flutter analyze` - passes with no errors
Compilation succeeds
  </verify>
  <done>
DI config includes HiveMigrationService registration, app compiles successfully, migration service is ready to run on user devices
  </done>
</task>

</tasks>

<verification>
**Code verification:**
- HiveMigrationService uses @lazySingleton
- SharedPreferences keys defined as constants
- Directory.delete(recursive: true) used for cleanup
- Retry logic with max 1 attempt
- main.dart calls migration service with .catchError()

**Integration verification:**
- Migration runs after configureDependencies() in main.dart
- Migration runs before CacheCleanupService
- Non-blocking pattern using Future.catchError()

**Build verification:**
- DI regenerated successfully
- No compilation errors
- No analyzer warnings
</verification>

<success_criteria>
**Measurable completion:**
1. `lib/core/migration/hive_migration_service.dart` exists with 60+ lines
2. HiveMigrationService is @lazySingleton with runMigration() method
3. Migration uses SharedPreferences for status tracking (hive_migration_complete key)
4. Migration deletes Hive directory using Directory.delete(recursive: true)
5. main.dart integrates migration service with .catchError() pattern
6. DI config includes HiveMigrationService registration
7. `flutter analyze` passes with no errors
8. App compiles and runs successfully
</success_criteria>

<output>
After completion, create `.planning/phases/08-migration-cleanup/08-01-SUMMARY.md`
</output>
