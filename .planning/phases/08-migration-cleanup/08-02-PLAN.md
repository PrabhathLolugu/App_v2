---
phase: 08-migration-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - lib/features/stories/data/models/story_model.dart
  - lib/features/social/data/models/user_model.dart
  - lib/features/social/data/models/comment_model.dart
  - lib/features/social/data/models/like_model.dart
  - lib/features/social/data/models/share_model.dart
  - lib/features/chat/data/models/conversation_model.dart
  - lib/features/chat/data/models/message_model.dart
  - lib/features/notifications/data/models/notification_model.dart
  - lib/features/home/data/models/activity_item_model.dart
  - lib/features/social/domain/entities/share.dart
  - lib/features/chat/domain/entities/message.dart
  - lib/features/notifications/domain/entities/notification.dart
  - lib/services/reading_progress_service.dart
  - lib/features/stories/data/datasources/story_local_data_source.dart
  - lib/features/home/data/datasources/activity_local_datasource.dart
  - lib/main.dart
  - pubspec.yaml
autonomous: true

must_haves:
  truths:
    - "No Hive imports remain in any source files"
    - "No @HiveType or @HiveField annotations remain in models"
    - "HiveService class is deleted from codebase"
    - "hive_registrar.g.dart is deleted from codebase"
    - "Hive packages are removed from pubspec.yaml"
    - "App compiles and runs without any Hive dependencies"
  artifacts:
    - path: "lib/core/storage/hive_service.dart"
      provides: "DELETED - no longer exists"
      exists: false
    - path: "lib/hive_registrar.g.dart"
      provides: "DELETED - no longer exists"
      exists: false
    - path: "pubspec.yaml"
      provides: "No hive_ce packages"
      not_contains: "hive_ce"
  key_links:
    - from: "lib/main.dart"
      to: "initHive()"
      via: "REMOVED - no longer called"
      pattern: "^(?!.*initHive).*$"
    - from: "all models"
      to: "hive imports"
      via: "REMOVED - no hive_ce imports"
      pattern: "^(?!.*import.*hive).*$"
---

<objective>
Remove all Hive code, packages, and dependencies from the codebase.

Purpose: Complete the Hive → Brick migration by eliminating all legacy Hive code, leaving only Brick-based storage.
Output: Clean codebase with zero Hive dependencies, all models use only Brick/Freezed annotations, app compiles successfully.
</objective>

<execution_context>
@/Users/amansikarwar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amansikarwar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-migration-cleanup/08-CONTEXT.md
@.planning/phases/08-migration-cleanup/08-RESEARCH.md
@.planning/phases/08-migration-cleanup/08-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Remove Hive annotations and imports from all models and entities</name>
  <files>
lib/features/stories/data/models/story_model.dart
lib/features/social/data/models/user_model.dart
lib/features/social/data/models/comment_model.dart
lib/features/social/data/models/like_model.dart
lib/features/social/data/models/share_model.dart
lib/features/chat/data/models/conversation_model.dart
lib/features/chat/data/models/message_model.dart
lib/features/notifications/data/models/notification_model.dart
lib/features/home/data/models/activity_item_model.dart
lib/features/social/domain/entities/share.dart
lib/features/chat/domain/entities/message.dart
lib/features/notifications/domain/entities/notification.dart
lib/services/reading_progress_service.dart
  </files>
  <action>
For each file listed, systematically remove all Hive-related code:

1. **Remove imports:**
   - Delete any `import 'package:hive_ce/hive.dart';` lines
   - Delete any `import 'package:hive_ce_flutter/hive_flutter.dart';` lines

2. **Remove annotations:**
   - Delete `@HiveType(typeId: N)` from class declarations
   - Delete all `@HiveField(N)` from field/parameter declarations
   - Keep all other annotations (@freezed, @JsonSerializable, @OfflineFirst, etc.)

3. **Example transformation:**
```dart
// BEFORE:
import 'package:hive_ce/hive.dart';

@freezed
@HiveType(typeId: 0)
class StoryModel with _$StoryModel {
  const factory StoryModel({
    @HiveField(0) required String id,
    @HiveField(1) required String title,
  }) = _StoryModel;
}

// AFTER:
@freezed
class StoryModel with _$StoryModel {
  const factory StoryModel({
    required String id,
    required String title,
  }) = _StoryModel;
}
```

4. Process all 13 files systematically
5. DO NOT modify any other code (keep JSON serialization, Brick annotations, domain logic)

Follow the exact pattern from RESEARCH.md "Pattern 5: Model Annotation Removal"
  </action>
  <verify>
Run `grep -r "import.*hive" lib/` - should return 0 results (except in planning docs)
Run `grep -r "@HiveType\|@HiveField" lib/` - should return 0 results (except in planning docs, generated files)
All 13 files still exist and have no compilation errors
  </verify>
  <done>
All models and entities have zero Hive imports, zero Hive annotations, all other code intact
  </done>
</task>

<task type="auto">
  <name>Remove HiveService usage and delete Hive infrastructure files</name>
  <files>
lib/features/stories/data/datasources/story_local_data_source.dart
lib/features/home/data/datasources/activity_local_datasource.dart
lib/core/storage/hive_service.dart
lib/hive_registrar.g.dart
lib/main.dart
  </files>
  <action>
Remove all Hive infrastructure in this order to avoid broken references:

**Step 1: Remove HiveService from data sources**
- In `story_local_data_source.dart`: Remove HiveService from constructor, remove any HiveService method calls
- In `activity_local_datasource.dart`: Remove HiveService from constructor, remove any HiveService method calls
- These data sources should now use ONLY Brick repository (MyItihasRepository)

**Step 2: Remove Hive initialization from main.dart**
- Delete the import: `import 'package:myitihas/core/storage/hive_service.dart';`
- Delete the line: `await initHive();` (around line 35)
- Keep all other initialization (Brick, DI, migration service, cache cleanup)

**Step 3: Delete Hive infrastructure files**
- Delete file: `lib/core/storage/hive_service.dart` entirely
- Delete file: `lib/hive_registrar.g.dart` entirely
- Delete the directory `lib/core/storage/` if it becomes empty

**Verification at this step:**
- No files import HiveService
- No files call initHive()
- hive_service.dart and hive_registrar.g.dart do not exist
  </action>
  <verify>
Run `ls lib/core/storage/hive_service.dart` - should return "No such file"
Run `ls lib/hive_registrar.g.dart` - should return "No such file"
Run `grep -r "HiveService" lib/` - should return 0 results
Run `grep "initHive" lib/main.dart` - should return 0 results
  </verify>
  <done>
HiveService deleted, hive_registrar.g.dart deleted, no code references HiveService or initHive
  </done>
</task>

<task type="auto">
  <name>Regenerate code and remove Hive packages</name>
  <files>
pubspec.yaml
lib/core/di/injection_container.config.dart
  </files>
  <action>
Clean up generated code and remove Hive packages in this specific order:

**Step 1: Clean generated files**
```bash
flutter packages pub run build_runner clean
```

**Step 2: Regenerate all code**
```bash
dart run build_runner build --delete-conflicting-outputs
```
This regenerates DI config (removes HiveService binding), freezed models (without Hive adapters), and Brick adapters.

**Step 3: Remove Hive packages from pubspec.yaml**
Edit `pubspec.yaml`:
- Remove from dependencies (around line 34-35):
  - `hive_ce: ^2.15.1`
  - `hive_ce_flutter: ^2.3.3`
- Remove from dev_dependencies (around line 93):
  - `hive_ce_generator: ^1.10.1`

**Step 4: Update dependencies**
```bash
flutter pub get
```

**Step 5: Final verification**
```bash
flutter analyze
```
Should pass with zero errors. If there are errors about missing Hive imports, check that all files were properly cleaned in Task 1.

**Step 6: Test compilation**
```bash
flutter build apk --debug
```
Or just `flutter run` to confirm app launches successfully.

Follow critical order from RESEARCH.md section "Critical implementation order" steps 7-11.
  </action>
  <verify>
Run `grep -i "hive_ce" pubspec.yaml` - should return 0 results
Run `flutter analyze` - passes with 0 errors
Run `grep "HiveService" lib/core/di/injection_container.config.dart` - should return 0 results
App compiles successfully (flutter build or flutter run succeeds)
  </verify>
  <done>
pubspec.yaml has zero hive_ce packages, DI config has no HiveService registration, all code regenerated successfully, flutter analyze passes, app compiles and runs
  </done>
</task>

</tasks>

<verification>
**Code cleanup verification:**
- Zero files with `import 'package:hive_ce/hive.dart'`
- Zero files with @HiveType or @HiveField annotations
- lib/core/storage/hive_service.dart does not exist
- lib/hive_registrar.g.dart does not exist
- pubspec.yaml contains no hive_ce packages

**Build verification:**
- `flutter packages pub run build_runner clean` completed
- `dart run build_runner build --delete-conflicting-outputs` completed successfully
- injection_container.config.dart has no HiveService references
- `flutter pub get` completed successfully
- `flutter analyze` passes with 0 errors
- App compiles (flutter build apk --debug or flutter run succeeds)

**Runtime verification:**
- App launches successfully
- No Hive-related errors in logs
- Migration service from Plan 01 still runs correctly
</verification>

<success_criteria>
**Measurable completion:**
1. Zero Hive imports in lib/ directory (excluding .planning/)
2. Zero @HiveType or @HiveField annotations in lib/ directory
3. lib/core/storage/hive_service.dart file does not exist
4. lib/hive_registrar.g.dart file does not exist
5. pubspec.yaml contains zero hive_ce packages (dependencies or dev_dependencies)
6. lib/main.dart does not call initHive()
7. injection_container.config.dart has no HiveService registration
8. `flutter analyze` returns 0 errors
9. App compiles successfully
10. App runs without Hive-related errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-migration-cleanup/08-02-SUMMARY.md`
</output>
