---
phase: 07-offline-ux
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - lib/core/presentation/widgets/offline_aware_button.dart
  - lib/core/presentation/widgets/offline_aware_icon_button.dart
autonomous: true

must_haves:
  truths:
    - "Buttons are visually disabled when offline (greyscale + opacity)"
    - "Tapping disabled button shows helpful snackbar explaining why"
    - "Buttons work normally when online"
    - "Snackbar message is short and technical (e.g., 'Not available offline')"
  artifacts:
    - path: "lib/core/presentation/widgets/offline_aware_button.dart"
      provides: "Elevated button wrapper with offline awareness"
      exports: ["OfflineAwareButton"]
      min_lines: 40
    - path: "lib/core/presentation/widgets/offline_aware_icon_button.dart"
      provides: "Icon button wrapper with offline awareness"
      exports: ["OfflineAwareIconButton"]
      min_lines: 40
  key_links:
    - from: "lib/core/presentation/widgets/offline_aware_button.dart"
      to: "lib/core/presentation/bloc/connectivity_bloc.dart"
      via: "BlocBuilder for state"
      pattern: "BlocBuilder<ConnectivityBloc"
    - from: "lib/core/presentation/widgets/offline_aware_icon_button.dart"
      to: "lib/core/presentation/bloc/connectivity_bloc.dart"
      via: "BlocBuilder for state"
      pattern: "BlocBuilder<ConnectivityBloc"
---

<objective>
Create reusable offline-aware button widgets that disable when offline and show helpful feedback

Purpose: Standardize write-action UX across app with greyscale disabled state and snackbar explanation
Output: OfflineAwareButton and OfflineAwareIconButton widgets
</objective>

<execution_context>
@/Users/amansikarwar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amansikarwar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-offline-ux/07-CONTEXT.md
@.planning/phases/07-offline-ux/07-RESEARCH.md
@.planning/phases/07-offline-ux/07-01-SUMMARY.md

# Pattern reference
@lib/core/presentation/bloc/connectivity_bloc.dart
</context>

<tasks>

<task type="auto">
  <name>Create OfflineAwareButton widget</name>
  <files>lib/core/presentation/widgets/offline_aware_button.dart</files>
  <action>
Create OfflineAwareButton that wraps ElevatedButton with connectivity awareness.

**Structure:**
- StatelessWidget with parameters:
  - required VoidCallback? onPressed
  - required Widget child
  - String offlineMessage (default: 'Not available offline')
  - ButtonStyle? style (optional, for customization)

**Build logic:**
- Return BlocBuilder&lt;ConnectivityBloc, ConnectivityState&gt;
- Check if state is ConnectivityOnline: `final isOnline = state is ConnectivityOnline`
- Return ElevatedButton:
  - onPressed: if isOnline, use provided onPressed. If offline, show SnackBar with offlineMessage, backgroundColor: Theme.of(context).colorScheme.error, duration: 2 seconds
  - style: Merge provided style with offline styling: backgroundColor: Colors.grey.withOpacity(0.5), foregroundColor: Colors.grey.shade700 (only when offline)
  - child: Wrap in Opacity widget with opacity: isOnline ? 1.0 : 0.5

Import: flutter/material, flutter_bloc, connectivity_bloc/connectivity_state.
  </action>
  <verify>flutter analyze lib/core/presentation/widgets/offline_aware_button.dart</verify>
  <done>OfflineAwareButton exists, uses BlocBuilder, shows greyscale when offline, shows SnackBar on tap when offline, no errors</done>
</task>

<task type="auto">
  <name>Create OfflineAwareIconButton widget</name>
  <files>lib/core/presentation/widgets/offline_aware_icon_button.dart</files>
  <action>
Create OfflineAwareIconButton for icon-based actions (like, comment, share buttons).

**Structure:**
- StatelessWidget with parameters:
  - required VoidCallback? onPressed
  - required Widget icon
  - String offlineMessage (default: 'Offline mode')
  - Color? color (optional)
  - double? iconSize (optional)

**Build logic:**
- Return BlocBuilder&lt;ConnectivityBloc, ConnectivityState&gt;
- Check if state is ConnectivityOnline: `final isOnline = state is ConnectivityOnline`
- Return IconButton:
  - onPressed: if isOnline, use provided onPressed. If offline, show SnackBar (same as button widget)
  - icon: Wrap in Opacity with opacity: isOnline ? 1.0 : 0.5
  - color: if offline, use Colors.grey, otherwise use provided color
  - iconSize: pass through

Import: flutter/material, flutter_bloc, connectivity_bloc/connectivity_state.
  </action>
  <verify>flutter analyze lib/core/presentation/widgets/offline_aware_icon_button.dart</verify>
  <done>OfflineAwareIconButton exists, uses BlocBuilder, greyscale when offline, snackbar on tap when offline, no errors</done>
</task>

</tasks>

<verification>
Analyze widgets:
```bash
flutter analyze lib/core/presentation/widgets/
```

Manual test (requires Plan 01 complete):
1. Run app with airplane mode OFF
2. Navigate to any screen with buttons
3. Temporarily replace ElevatedButton with OfflineAwareButton
4. Button appears normal, taps work
5. Enable airplane mode, wait 3 seconds
6. Button appears greyscale with reduced opacity
7. Tap button → SnackBar shows "Not available offline"
</verification>

<success_criteria>
- [ ] OfflineAwareButton created with BlocBuilder, greyscale styling when offline, SnackBar on tap
- [ ] OfflineAwareIconButton created with same offline behavior for icon buttons
- [ ] Both widgets use ConnectivityBloc via BlocBuilder
- [ ] No flutter analyze errors
- [ ] Widgets are ready for integration in Plan 03
</success_criteria>

<output>
After completion, create `.planning/phases/07-offline-ux/07-02-SUMMARY.md`
</output>
