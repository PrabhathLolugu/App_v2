/// Model for signup data
/// This is used only for collecting user input before signup
class SignupData {
  final String email;
  final String fullName;
  final String password;

  SignupData({
    required this.email,
    required this.fullName,
    required this.password,
  });
}

